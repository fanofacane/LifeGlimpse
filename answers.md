# LifeGlimpse 技术问答

## 1. 视频热度计算中的半衰期算法
- 核心思想：采用指数衰减 `score = baseScore * (1/2)^(Δt / halfLife)`，让近期事件贡献更大、旧事件逐步减弱。
- 事件构成：为播放、点赞、评论、分享等行为设定基准分，应用半衰因子后累加成视频总热度。
- 实施流程：配置半衰期与权重 → 计算时间差 → 得到事件得分 → 按视频聚合 → 写入 Redis/ZSet 供榜单与推荐使用 → 依据反馈调优参数。
- 选择理由：兼顾即时性与积累、参数直观易调、避免窗口截断跳变、实现成本低并具可解释性，同时具备一定抗刷量能力。

## 2. Feed 流关注推送中 Redis ZSet 的 score 设计与历史回填
- score 设计：常用秒/毫秒级时间戳；若需稳定排序，可叠加本地自增序列或轻量质量偏移，保证新鲜度优先。
- 数据维护：定期设置 TTL 清理过期内容，通过 `ZRANGE`/`ZREVRANGE` 分页读取；ZSet value 使用内容 ID 自带去重。
- 新关注回填：在关注动作触发回填任务，从存储层拉取被关注者最近 N 条内容，按原始发布时间批量 `ZADD` 到粉丝 ZSet。
- 批量写入与幂等：使用 pipeline/multi，控制回填批量，过滤状态失效内容，若有撤回则 `ZREM`；必要时异步处理确保体验。

## 3. 基于 Netty 的实时聊天可靠投递与顺序保障
- 消息唯一性：为每条消息生成 `messageId` 并先落库，客户端按 `messageId` 去重；同会话维护递增 `sequence`。
- ACK 与重发：客户端确认后回传 ACK，服务端维护未确认表并定时检测超时重发；断线重连带上最后确认 `seq` 以补齐缺漏。
- 顺序控制：按会话一致性哈希绑定业务线程，客户端按 `seq` 缓存乱序消息等待补齐；写失败通过 `ChannelFutureListener` 标记重投。
- 离线与多端：离线消息持久化，用户上线按最后已读 `seq` 拉取并逐条 ACK；监控 ACK 延迟与重试次数，必要时触发补偿流程。

## 4. 基于标签的用户兴趣得分与推荐
- 事件权重：对播放、完播、点赞、评论、分享、收藏等行为设定正负权重，举报等负反馈扣分。
- 时效衰减：对每次事件应用半衰因子 `score = baseWeight * 0.5^(Δt / halfLifeTag)`，热门标签半衰期可更短。
- 汇总策略：日志记录用户-视频-标签行为，定时或实时累加形成 `userTagScore`，可采用 `score = score_old * decay + score_event` 滑动更新。
- 推荐应用：按用户标签 Top-K 召回候选集，将标签得分作为排序特征，辅以探索策略引入新标签内容；得分存储于 Redis Hash 或 MySQL，定期监控分布与兴趣漂移。

> 以上内容汇总自此前关于热度算法、Feed 推送、Netty 聊天与标签推荐的答复，便于统一查阅。

